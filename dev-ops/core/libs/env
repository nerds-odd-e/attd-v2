. ${dev_ops_path}/core/main.env

bin_file=$(basename "$0")

cd "${dev_ops_path}/envs/${bin_file#env-}"

if [ "${1}" = ggg ]; then
	shift
	times=1
	while ! docker-compose "$@"
	do
		echo "Retry $((times++)) times..."
	done
else
	docker-compose "$@"
fi

